{
  "name": "denshi-craft-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:worker": "wrangler dev",
    "deploy:worker": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy frontend --project-name lchika-no-tsuzuki"
  },
  "devDependencies": {
    "wrangler": "^4.15.0"
  }
}
